import { css } from '@emotion/react';

export const resets = css`
  html, body, #root {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
  }

  h1 {
    font-size: 4.5rem;
  }

  button {
    cursor: pointer;
  }
`;